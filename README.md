# 🚀 Task Management System (Backend)

این پروژه بخش بک‌اِند سامانه مدیریت تسک‌ها است که با استفاده از فریم‌ورک **NestJS** توسعه یافته است. هدف این پروژه مدیریت کاربران، احراز هویت و مدیریت وظایف (Tasks) با رعایت اصول معماری لایه‌بندی شده است.

## 🛠 Tech Stack

* **Framework:** NestJS
* **Language:** TypeScript
* **Database:** PostgreSQL (with TypeORM)
* **Security:** JWT (JSON Web Tokens), Passport.js, Bcrypt
* **Validation:** Class-validator & Class-transformer
* **Documentation:** Swagger UI (Optional)

## 🔑 Key Features

1.  **Authentication & Authorization:**
    * ثبت‌نام و ورود کاربر با استفاده از JWT.
    * هش کردن رمز عبور با `Bcrypt` برای امنیت حداکثری.
    * استفاده از `JwtAuthGuard` برای محافظت از مسیرهای حساس.

2.  **Task Management (CRUD):**
    * ایجاد، ویرایش، حذف و نمایش تسک‌ها.
    * ارتباط منطقی بین تسک‌ها و کاربران (One-to-Many).

3.  **Data Validation:**
    * استفاده از **DTOs** برای اعتبارسنجی دقیق داده‌های ورودی.

## 📂 Project Structure

```text
src/
├── auth/           # منطق احراز هویت (Login, Register, JWT Strategy)
├── tasks/          # ماژول مدیریت تسک‌ها (Controller, Service, Entity)
├── users/          # مدیریت اطلاعات کاربران
├── common/         # اینترفیس‌ها، گاردها و دکوراتورهای عمومی
└── main.ts         # نقطه شروع اپلیکیشن
